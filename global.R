require(qtlcharts)
require(qtl)
require(markdown)
require(ggplot2)
require(qtl)      
require(ASMap)
require(shinythemes)
data(hyper)



# mydata <- read.cross(format = "csv",
#            file = "data/cross.csv" ,
#            genotypes = c("a","h","b"),
#            alleles = c("a","b"),
#            estimate.map = FALSE,
#            BC.gen = 0,
#            F.gen = 6
# )